# Todo
- Maybe need a 'track' system for each person, which would be an Array of their path to their respective WinPosition. i.e. `[1, 2, 3 ... R3]`
    - Remember to remove the `inLeg` crap from the `Player.java` class when done
    - Could be part of a `TrackFactory` class that generates the track for each player. Maybe move the `WinPosition` generation to this class as well
- Add colouring for each player to the console output